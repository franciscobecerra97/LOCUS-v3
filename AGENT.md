# LOCUS Improvement Project Agent Entry

This file exists because the project owner explicitly requested `AGENT.md`.
Codex automatically follows `AGENTS.md`; therefore `AGENTS.md` is the
authoritative project instruction file.

Before doing any work:

1. Read `AGENTS.md` completely.
2. Read `PROJECT-CHARTER.md`, `BASELINE.md`, and `PLAN.md`.
3. Check `DECISIONS.md` for unresolved owner decisions.
4. Read the technical contract relevant to the task.
5. Preserve the original paper thesis and all frozen protocol identifiers.
6. Use only synthetic data and project-controlled disposable services.
7. For P8 or later implementation, testing, evidence, or artifact work, read
   `prototype_final/README.md` and work from `prototype_final/`; treat the root
   implementations and commands as historical/component controls. P7.7 is
   complete, and P8.1/P8.2 assurance is complete. P8.3 is the next proposed
   step; further retained collection remains blocked until each applicable
   schema and evidence gate passes.
8. Do not modify the external upstream checkout. Treat `paper/` here as the
   authoritative continuation manuscript, but edit it only after presenting
   the exact delta and receiving explicit owner approval.

If this file and `AGENTS.md` ever disagree, stop and follow `AGENTS.md`.
