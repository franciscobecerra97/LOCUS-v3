# LOCUS Claim-Evidence Matrix

Status date: 2026-07-24. This document gates manuscript claims for the ASIACCS 2027 Cycle 1 submission.

## Evidence-State Rules

- `Supported`: the claim has evidence matching its stated scope and can be reproduced.
- `Partial`: some relevant design, implementation, proof, test, or citation exists, but the complete paper-facing claim is not yet supported.
- `Planned`: the claim is an intended result with a defined evidence path but no adequate current result.
- `To remove`: the wording is unsupported, misleading, or outside the selected study and must not remain as a paper claim.

Passing a unit test supports only the tested implementation behavior. It does not establish a cryptographic theorem, distributed-system property, usability result, or production readiness.

## Claim Matrix

| ID | Paper claim and location | Exact property and adversary | Provenance | State | Current evidence | Required evidence or paper action | Residual risk / limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CLM-01 | Abstract and Introduction contribution 1: LOCUS separates encrypted cloud backup storage from threshold recovery-party state. | A cloud snapshot lacks party shares; a party snapshot lacks the encrypted key and whole recovery secret. | LOCUS architecture | `Partial` | `paper/main.tex` construction and state table; strict filesystem and S3-compatible adapters; and one default Compose gate combining SeaweedFS, five parties, a resolver, and an ephemeral client. The resolved and live graph has disjoint internal networks, five pairwise-disjoint party volumes, a separate cloud volume, no host ports, and no cloud credential at parties. Recursive provisioning audits exclude every encoded party state/key from the client bundle and ciphertext from party configurations; recovery succeeds before and after party restart. The retained P6.2-P6.4 records validate the exact cloud snapshot, every persistent file in one stopped party-1 volume, and their matching manifest-bound union under separate networkless candidate processes. | Add process-memory/trace inspection where required by final wording and reproduce on a clean host. VM/multi-host and real-provider behavior remain optional non-claims. | All roles and the privileged provisioner still share one trusted host/engine; exact filesystem snapshots do not cover memory, deleted blocks, logs, cumulative compromise, or independent cloud/party administration. |
| CLM-02 | Abstract and Introduction contribution 2: the public cue-policy abstraction permits reproduction without storing raw cues, cue identifiers, password verifiers, recovered secrets, or wrapping keys at cloud/parties. | Cloud and party persistent records exclude prohibited client-local cue and key material. | LOCUS cue policy and storage design | `Partial` | `docs/cue-policy.md` freezes the exact three-pair contract; the current v4/v2 deployment authenticates the exact `LOCUS-location-person-set-v1` identifier and rejects legacy or mixed profiles before authorization. The default deployment resolves only a synthetic fixture, canonicalizes it in the client, recursively audits role bundles, inspects runtime mounts/environments, and rejects known cue, credential, private-key, signer-key, and TPASS-state markers in collected output. The fixture is bound to one exact 511-byte canonical vector. The three retained v2 snapshot records bind clean commit `12ca815`, passed the registered state-boundary checks, output scans, and cleanup, and expose no fixture or S3 credentials to parties. | Reproduce on clean Linux/Windows CI, add process-memory/crash-dump/trace analysis, external-resolver traces, and metadata-leakage experiments. | The old Cycle 1 v1 corpus binds the superseded legacy label and is historical only. The trusted provisioner sees bootstrap state; application output checks do not erase runtime memory or constrain a privileged host; external resolver observations and endpoint compromise remain outside this evidence. |
| CLM-03 | Claim `correctness`: correct canonical recovery input, enrolled backup, and at least `t` valid parties return the private key. | Functional correctness under honest components and matching epoch/policy. | TPASS correctness plus LOCUS composition | `Partial` | Complete local LOCUS-flow tests use native Rust/Ristretto TPASS, HKDF-SHA-256, and AES-256-GCM for `(2,3)`, `(3,5)`, and `(5,9)`. The default Compose gate provisions a fresh synthetic three-pair client, immutably stores/reads the backup through SeaweedFS, authorizes through five mTLS parties, reconstructs native 2-of-3 over `[1,3]`, decrypts the key, restarts party 1, and repeats at the next exact attempt slot. Direct native/Rust/AEAD and alternate-subset/process tests also pass. | Add clean-host/cross-platform deployment reproduction, parameterized deployed subsets and failures, lifecycle/re-enrollment, and independent review. | This is deterministic same-host synthetic evidence; resolver drift, endpoint failures, authorization-quorum loss, and malicious parties can prevent recovery. |
| CLM-04 | TPASS interface and prototype evaluation: any valid threshold subset can recover. | Any selected set of at least `t` valid enrolled parties reconstructs the same TPASS group secret. | Inherited TPASS property and implementation | `Partial` | Native complete-flow tests cover non-contiguous subsets across `(2,3)`, `(3,5)`, and `(5,9)`; the Rust property matrix exhausts every valid subset for `1 <= t <= n <= 5`, including reversed input ordering; authenticated subprocess recovery succeeds with `[1,3]` and `[2,3]`, including `[2,3]` while party 1 is down. | Add parameterized distributed-service subset coverage, below-threshold failures at the public interface, and deployment repetition. | Attempt-control authorization quorum may be larger than `t`, reducing operational availability even though TPASS needs only `t`. |
| CLM-05 | Claim `cloud-only`: cloud compromise does not yield decryption or an offline cue-testing oracle. | From the cloud backup alone, guessed recovery inputs have no local correctness predicate. | LOCUS state separation plus AEAD/active-suite assumptions | `Partial` | Construction argument; pinned AES-256-GCM/HKDF implementation; bounded canonical cloud-object format; and filesystem/live S3-compatible contract tests. The retained Yi P6.2 record binds the exact two-file S3 snapshot and observes two synthetic candidates with zero candidate signals, network attempts, excluded-path accesses, or prohibited-material findings in a separate non-root, credential-free, read-only, networkless container. Positive controls detect an injected verifier and file/network attempts. P5A.6 adds a non-retained aggregate development regression over both backup-v5 suite branches with a direct-verifier positive control. | Collect new suite-specific evidence only after the P8/P9 schema/path gates; inspect remaining memory/log/trace surfaces; reproduce on clean hosts; and assess real-provider behavior separately. | P5A.6 is component regression, not retained evidence or cryptographic proof. Public policy metadata, versions, object size, locator, and access timing remain visible; deletion, online recovery attempts, host memory, deleted blocks, provider internals, and multi-epoch correlation remain possible or unevaluated. |
| CLM-06 | Claim `below-threshold`: fewer than `k` compromised holders cannot reconstruct the recovery output or test cues offline under the selected suite's assumptions. | Coalition receives fewer than `k` matching suite records and public metadata. | Inherited Yi/aPPSS security plus LOCUS record design | `Partial` | Yi et al. and aPPSS source assumptions remain separate. The retained Yi P6.3 record covers one stopped party snapshot in the deployed 2-of-3 profile. P5A.6 additionally checks every empty/single-holder coalition for both serialized 2-of-3 suite formats under one fixed networkless aggregate regression; ordinary views expose no tested predicate and transient verifier injection is detected. D020's internal mapping assessment provisionally accepts the scoped Yi/aPPSS boundary with explicit qualifications and no correction-required finding. | Obtain D019 independent human confirmation; collect suite-specific retained evidence after P8/P9; inspect live memory/log/trace surfaces; and reproduce on clean hosts. | The D020 assessment is not independent and P5A.6 tests exact component behavior rather than proving either theorem. The retained record remains Yi-only. Live/adaptive compromise, side channels, other thresholds, multiple/cumulative or cross-epoch compromise, and compromise at `k` remain outside the below-threshold claim. |
| CLM-07 | Claim `combined`: cloud plus fewer than `k` matching holders still gives no offline cue-testing oracle under the selected suite's assumptions. | Adversary combines ciphertext/public policy with a below-threshold serialized coalition. | LOCUS composition plus the selected inherited suite | `Partial` | The retained Yi P6.4 record covers one exact cloud-plus-party union. P5A.6 adds all four matching below-threshold coalitions for each backup-v5 suite branch under a fixed aggregate networkless component regression, with a direct-verifier positive control and no retained per-input result. D020's internal assessment provisionally accepts the outer composition with explicit qualifications and no correction-required finding. | Collect suite-specific retained evidence after P8/P9, obtain D019 independent human mapping confirmation, inspect remaining surfaces, and reproduce on clean hosts. | The D020 assessment is not independent; P5A.6 is not proof, cryptanalysis, or retained evidence. Live memory, traces, side channels, other thresholds/profiles/epochs, cumulative compromise, cloud plus `k` holders, endpoint compromise, online guessing, and metadata leakage remain possible or untested. |
| CLM-08 | Claim `tamper`: cloud substitution, corruption, and rollback are detected when honest current intended-epoch party metadata is available. | Client rejects a cloud object whose digest/epoch does not match honest current party state; AEAD rejects ciphertext modification. | LOCUS binding and AEAD | `Partial` | Immutable `(bid, epoch)` filesystem objects and S3 conditional writes plus exact references bind a full backup digest; signed configuration and durable party state pin that digest. Lifecycle/process/Compose tests reject selected cloud/party cross-mixes, preserve the tested partial split, restart activated state, refuse the retired predecessor, and recover under the successor. Fake/live S3 and broader tests reject immutable-key conflict, noncanonical/oversized data, stale substitution, corruption, mismatched references, and ciphertext/AAD tamper. | Reproduce on a clean host and retain the current-honest-metadata condition. | Deletion remains availability loss. This is cloud-object binding, not party-state rollback resistance: P5.13 shows restored party state can fork attempt/lifecycle history, and rollback of cloud plus all authoritative party metadata can present a consistent old view. |
| CLM-09 | Error-channel text: wrong cues and malformed inputs fail generically without creating a cheaper oracle. | External clients receive a normalized rejection; parties do not learn the final TPASS/AEAD success result. | LOCUS service behavior | `Partial` | Existing Rust/Python negatives and HTTPS generic errors remain. P4.1 adds an exact recovery phase machine whose retry state/events contain no suite/AEAD result and a single `recovery rejected` normalization for wrong input and malformed secret-path state. The local state machine is never sent to parties. | Integrate the P4.1 boundary into a newly versioned end-to-end client path; add timing/size/leakage analysis, redacted-log inspection, and broader malicious-party experiments. An optional OIDC adapter requires separate evidence. | Pre-attempt availability errors may remain distinct. A counted session remains an online correctness oracle to its authorized client; component tests are not deployment or cryptographic proof. |
| CLM-10 | Claim `online-guessing`: conditional success is at most `min(1, k*2^-h)` for at most `k` evaluated guesses. | Analytic union bound given conditional min-entropy `h` and an independently enforced deployment bound of at most `k` online evaluations. | Mathematical model | `Supported` only as a conditional bound | Elementary union-bound argument in the manuscript. | Preserve the conditional wording and state that the scoped LOCUS prototype does not establish `k`; never substitute its configured local counter. | No measurement establishes `h` or `k`; adaptive side information and obvious cues may make `h` small, and deployment controls may permit more attempts. |
| CLM-11 | Former system claim: attempts are globally bounded across subsets, concurrency, retries, replay, crashes, and rollback. | For one `(backup_id, epoch)`, no more than effective budget `B_eff` distinct TPASS request transcripts obtain sufficient honest responses. | Former intended contribution | `To remove` as a positive paper claim | The prototype has useful local certificates, locks, exact retries, pre-response persistence, restart behavior, and same-membership lifecycle handling. P5.13 nevertheless finds conflicting 4-of-5 certificates after one honest database restore under quorum-only reconciliation. The shortest trace and strict report are reproducible with `tasks.py attempt-model`. | Remove global-bound wording from abstract, introduction, construction, evaluation, lifecycle, and conclusions. Retain the implemented local behaviors as implementation facts and the counterexample as a limitation/negative result. | No snapshot rollback resistance, public admission, arbitrary-schedule bound, or lifetime/cross-epoch budget exists. A monotonic witness or stronger consensus would change the architecture and is deferred. |
| CLM-12 | Former wording: recovery is “rate-limited,” “auditable,” “durable,” or artifact-backed as a security property. | Deployed parties enforce a rollback-resistant attempt property and retain privacy-minimized tamper-evident evidence. | Former intended operational mechanism | `To remove` as a positive paper claim | SQLite durability, redacted events, signed certificates, freshness, guarded native phases, and lifecycle transitions execute across tested same-host processes. These support only exact local implementation statements. | Use “partial local attempt-control instrumentation” or equivalent factual wording; state that deployment-provided rate limiting and abuse controls are assumptions outside the scoped result. | Same-host durability, signatures, TLS, lifecycle certificates, and audit chaining do not establish global rate limiting, rollback resistance, public authorization, or safe false-lockout recovery. |
| CLM-13 | Claim `resolver-boundary`: local canonicalization prevents cloud/party storage of raw cues but does not hide resolver queries. | Cloud and parties do not receive raw resolver inputs; an external resolver can observe queries, candidates, timing, and account metadata. | LOCUS privacy-boundary analysis | `Partial` | Paper data-flow argument; `docs/cue-policy.md`; `docs/resolver-behavior.md`; and a deterministic HTTP resolver on its own internal network. Only the client joins that network; parties and S3 receive neither its fixture nor resolver connectivity, and the resolver suppresses request logs. A versioned simulation strips display/record metadata locally and covers stable rename/reindex changes, canonical coordinate/contact drift, profile change, ambiguity, and missing results. The registered `resolver-unavailable-v1` Compose scenario requests a nonexistent resolver path through the normal client and observes zero consumed attempts before/after. | Add external/local mode comparison, network traces proving zero party calls on local rejection, timing/size analysis, real resolver failure modes, and quantitative metadata-leakage analysis. | The bootstrap scenario establishes one early failure boundary, not resolver privacy, constant-time behavior, or real-provider stability; real observations can substantially improve online candidate ranking. |
| CLM-14 | Claim `authority`: no single implemented storage/recovery role can recover the private key alone. | Cloud, one party, resolver, or stateless coordinator individually lacks at least one required factor/state component. | LOCUS architecture | `Partial` | Existing retained role snapshots remain unchanged. P4.2 additionally removes Client A's credential root, starts Client B in a distinct sanitized subprocess/root with a fresh identity and no party state, recovers through an authenticated 2-of-3 subset, and matches external private/public fingerprints. An inherited-state positive control fails. | Reproduce the new isolation profile on clean Linux/Windows and integrate the P2.2/P3 admitted retrieval path end to end; keep future IdP/admin designs outside this claim. | The active client, trusted test host, or threshold party compromise defeats the boundary. Bounded same-host deletion/isolation is not forensic erasure or administrative independence. |
| CLM-15 | Scoped lifecycle statement: the tested same-membership transition prepares a direct successor, prevents the tested 3/2 partial activation from forming a quorum, retires the predecessor in current state, and recovers under the successor. | Exact tested cross-epoch mixes and current-state predecessor use are rejected; no general rollback/replacement claim. | LOCUS lifecycle mechanism | `Partial` | Canonical old/new approvals, package-bound readiness, durable transition locks, atomic local retirement/activation, coordinator-only pinned-mTLS routes, five-process tests, and `cross-epoch-runtime-mix-v1` cover the stated same-host path. P4.3 adds a secret-free durable publication journal, prepared-successor original-key verification, exact action retries, and synthetic crashes after every effect boundary. P5A.5 reuses that journal in an active application/component interface for Yi-to-Yi, aPPSS-to-aPPSS, Yi-to-aPPSS, and aPPSS-to-Yi, verifies the same protected-key digest before cutover, and rejects tested old/new and cross-suite mixtures. | Wire the P4.3/P5A.5 adapters to newly versioned P6 provider/party deployment profiles and keep the claim exactly test-scoped; add public replacement only after its separate gate. | P5.13 authorizes after restored pre-retirement snapshots under quorum-only reconciliation. P4.3/P5A.5 crash tests are component-level and synthetic. Restored volumes, general replacement, independent administration, public authorization, and global budget continuity are not defended. |
| CLM-16 | Appendix and implementation text: LOCUS implements Yi et al.'s zero-knowledge TPASS algebra over realistic parameters. | Enrollment and distributed message steps implement Protocol 2 equations over Ristretto255 with validated encodings and stated instantiation changes. | Inherited construction, new implementation mapping | `Partial` | `tpass-core/`; `tpass-python/`; native-default local and deployed LOCUS composition; authenticated commitment/response routes carrying canonical wire objects; `docs/crypto-design.md`; `docs/tpass-wire-format.md`; `docs/paper-protocol-mapping.md`; native vectors and regression coverage; pinned Cargo/uv dependencies; retained distributed Ristretto measurements; and D020's provisional internal mapping acceptance with explicit `G2`, group, transcript, and proof qualifications. | Obtain D019 independent human Yi mapping confirmation and anonymous clean-host reproduction. | The D020 assessment and vector are not independent validation; the deployment is synthetic same-host evidence. Source proof uses an explicit proof-of-knowledge assumption; transparent `G2` derivation changes setup. Not audited. |
| CLM-17 | Implementation contribution: separately identified party processes, cloud/resolver boundaries, service identities, durable state, and authenticated transport in the same-host research deployment. | Enrollment/recovery crosses the tested process and storage boundaries without implying independent administration or public admission. | LOCUS systems implementation | `Partial` | The frozen Compose path retains five non-root TLS 1.3 parties and separate volumes. P3.2 additionally starts clean subprocess services from `LOCUS-party-service-config-v2` files containing no native state, sends one recipient-bound initial Yi package over pinned mutual TLS, and verifies separate databases retain only their own state. P5A.3/P5A.4 component tests start three distinct aPPSS subprocesses with pinned mutual TLS, separate holder databases and public-only boot files; each process creates only its own OPRF key, all holders install one common public state, and correct/wrong recovery traverses strict P5A.1 messages. P5A.5 adds the active application/component exact-suite enrollment/recovery interface and all four same-suite/cross-suite successor directions through signed descriptors and deterministic bundles. Existing live Yi recovery/restart checks remain. | Integrate both suites into newly versioned P6 deployment/evidence profiles; add process-memory/trace inspection and clean-host reproduction. | The retained v2 deployment remains Yi-only and used its trusted bootstrap provisioner. P5A.3--P5A.5 are component behavior, not retained evidence. Synthetic one-host processes are not independent administration, public recovery authorization, or deployment-grade PKI. |
| CLM-18 | Evaluation: measured end-to-end cost and implemented-phase overhead for the frozen local profile. | Client-observed cost for the corrected same-host native `(2,3)` deployment, including correct recovery, fixed wrong-input rejection, one-party unavailability, phase timing, application bytes, and persistent bytes. | LOCUS evaluation | `Partial` | Ten retained v2 three-scenario blocks provide 30 samples per scenario from clean cutover commit `12ca815` and pseudonymous host `cycle1-v2-host-a`. P7.15 accepted the exact 30-file `compose-performance-v2` corpus and emitted canonical `LOCUS-performance-processed-v2` summary digest `462e492795fafdd90a4f39851a612275193603d816f761728afe05e97a470a6b`; P7.16 emitted matching manifest-bound v2 rows. Median enrollment was 250.030 ms; median correct, one-party-unavailable, and wrong-input recovery latencies were 442.063, 411.134, and 409.090 ms. | Reproduce on an anonymous clean host and keep interpretation limited to this exact profile. | The v1 values remain historical. One same-host synthetic collection does not establish acceptable cost, Internet-scale/production performance, scalability, concurrency, geographic behavior, independent administration, or a comparative advantage. |
| CLM-19 | Artifact/Open Science: scoped central results are reproducible from a clean environment. | Anonymous artifact can run core recovery/failure paths, the bounded negative model, retained attacks/benchmarks, and regenerate principal tables/figures. | LOCUS artifact | `Partial` | Frozen uv/Cargo dependencies, cross-platform tasks, strict metadata/report schemas, cue/drift corpora, repository hygiene, default Compose smoke, retained P6/P7 records, deterministic P7 raw-to-processed validation, provenance-bound processed-to-LaTeX generation, and `tasks.py attempt-model` provide the current paths. The retained table inputs regenerate from the exact committed corpus. | Add clean-machine/CI reproduction, anonymous packaging/link, and an independent reproducer. | The current retained collection is not yet anonymous or independently reproduced. The bounded model is a negative-result oracle, not a proof. |
| CLM-20 | R5 and cue-policy text: structured recovery input is deterministically reproducible across clients. | Same three logical cue selections under the same policy/version serialize to the same password input across supported platforms. | LOCUS cue-policy implementation | `Partial` | The frozen policy covers exactly three distinct pairs, all six orders, exact half-even coordinate rounding, strict email/E.164 normalization, duplicate/unknown-field rejection, and drift-sensitive output. A pinned corpus binds the deterministic HTTP fixture to one exact 511-byte encoding and SHA-256 digest and rejects locale-dependent decimal/phone forms plus Arabic-Indic, nonbreaking-space, decomposed, and fullwidth variants. A second corpus proves display rename and within-cell map reindexing preserve the bytes while across-cell/contact changes alter them and profile/ambiguity/missing results reject. A fresh deployment client consumes the baseline fixture and completes S3-backed recovery. | Execute both corpora on clean Linux/Windows CI and an alternate implementation/client; trace attempt behavior for drift/local rejection; add clean-host reproduction. | Vectors from the same implementation are regression evidence, not independent interoperability evidence. Human recall and real resolver stability remain distinct from deterministic encoding. |
| CLM-21 | Implementation, limitations, ethics, and related-work text: LOCUS makes no measured cue-memorability, comparative recall, or usability claim. | System evidence is limited to deterministic software processing; human retention/reproduction remains unevaluated. | Claim-scope audit | `Supported` | The manuscript now removes “human-memorable,” “personal-memorable,” claimed usability improvement, and comparative reproduction wording from LOCUS goals/contributions. It explicitly states that no user study exists, the ability to retain/resupply cues is unmeasured, prior human-memory work is motivation only, and future study requires ethics review. M4 verified every motivation source cited by the current manuscript. | Preserve this non-claim unless an approved human-subject study supplies matching evidence. | Synthetic vectors, resolver simulations, and software determinism cannot establish human memorability, entropy, or usability. |
| CLM-22 | Threshold availability: recovery works with `t` responsive parties and fails below threshold. | Honest recovery remains possible under specified party unavailability. | TPASS/system availability | `Partial` | Deterministic tests cover one slow/unavailable/malformed authorizer, two-authorizer quorum loss, stale-party exclusion, TPASS fallback, insufficient threshold, phase timeout, and fixed-set failure. Authenticated native recovery and the default Compose gate succeed with TPASS parties `[2,3]` while party/authorizer 1 is stopped, using the surviving compact 4-of-5 authorizer quorum; the Compose count advances exactly to three after prior healthy/restart recoveries. | Add parameterized authorization-versus-TPASS quorum analysis, arbitrary malicious-party network schedules, below-threshold public failure checks, and VM/container resilience distributions. | Authorization may need more than `t` reachable parties; compact 4-of-5 tolerates only one unavailable authorizer and denial of service remains possible. Same-host deterministic evidence is not Byzantine-liveness evidence. |
| CLM-23 | Planned Docker/CLI artifact: one command starts the scoped local system; smoke, benchmark, and retained attack modes use the same implementation. | Versioned Compose profiles reproduce same-host role separation and invoke the tested interfaces without prohibited output. | LOCUS reproducibility infrastructure | `Partial` | `deployment-smoke`, `deployment-demo`, `deployment-benchmark`, and `deployment-attack` build the same pinned image and exercise the same provisioner/client/resolver/S3/five-party graph. Resolved assertions check identities, mounts, networks, credentials, dependencies, and commands; recovery/restart/fallback and registered resolver/lifecycle profiles passed with dynamic canaries and cleanup. | Add only the scoped operator commands, retained attack/benchmark scenarios, clean-host/CI reproduction, artifact packaging, and independent reproduction. | One-host profiles validate common implementation paths but not realistic performance, independent administration, unclaimed attempt properties, or final artifact usability. |
| CLM-24 | A3 and distributed design: setup and recovery transport is authenticated and confidential. | Network attacker cannot read enrollment shares, impersonate roles, alter accepted protocol messages, or replay them as fresh sessions. | Operational security assumption and implementation | `Partial` | P3.2 adds recipient-bound enrollment over pinned mutual TLS. P3.4 adds deterministic Ed25519 admission capabilities, client proof signatures over exact request digests, independent digest-only replay stores, and pre-backend storage-gateway validation. Existing Yi ledger/freshness/recovery routes retain TLS 1.3, exact pins, bounded canonical messages, and HTTP idempotency. P5A.3/P5A.4 add certificate-pinned mutual-TLS aPPSS recovery, initialization, and install routes; services recompute exact public epoch/certificate bindings, commit caller/route/body idempotency before OPRF work, return restart-safe exact responses, and reject partial initialization as a complete result. P5A.5 consumes those routes behind descriptor-only suite dispatch and the existing crash-safe publication journal. | Integrate both suites into a newly versioned end-to-end deployment/evidence path after the complete P5A gate; add certificate rotation/revocation, arbitrary active-network scheduling, rollback tests, and trace inspection. | Component tests use synthetic keys on one host. The aPPSS routes and P5A.5 adapter are not yet the released admission/lifecycle-integrated deployment. Endpoint/issuer/key compromise, traffic analysis, denial of service, database rollback, external account recovery, and production abuse remain outside this result. |

## Abstract And Introduction Gate

The following mappings cover every substantive claim currently present in the abstract and contribution paragraph:

| Manuscript statement | Matrix IDs | Current action |
| --- | --- | --- |
| Structured cues combined with cloud backup and TPASS parties | CLM-01, CLM-03, CLM-17, CLM-20 | Retain as architecture; avoid implying complete distributed implementation. |
| Cue policy reproduces processing without storing raw cues/verifiers | CLM-02, CLM-13, CLM-20 | Qualify until service/database and cross-platform tests exist. |
| Cloud, below-threshold, and combined compromise provide no offline oracle | CLM-05, CLM-06, CLM-07 | Retain as assumption-scoped design claims backed by the bounded v2 snapshot experiments and D020 provisional internal assessment; complete clean-host reproduction and D019 independent human Yi/aPPSS/LOCUS validation. |
| Residual guesses are threshold-mediated | CLM-03, CLM-04, CLM-10 | Retain with TPASS assumptions. |
| Residual guesses are rate-limited and auditable | CLM-11, CLM-12 | `Done` Removed as a LOCUS system claim. Deployment controls are assumptions; the partial ledger and bounded counterexample are reported only as exact implementation/limitation facts. |
| Security analysis covers tamper detection and resolver privacy | CLM-08, CLM-09, CLM-13 | Retain as analysis; do not describe incomplete mechanisms as implemented. |
| Reference prototype and evaluation methodology | CLM-16, CLM-18, CLM-19 | Retain with explicit research/toy/current-state boundaries. |

## Required Claim Corrections

These are manuscript tasks, not evidence substitutions:

1. `Done` Remove present-tense “rate-limited,” “auditable,” and global-budget security wording; use “durable” only for exact tested local writes. CLM-11/12 are scoped non-claims, not Cycle 1 implementation gates.
2. `Done` CLM-21 cue-usability correction: remove LOCUS-specific “human-memorable,” “personal-memorable,” and unmeasured comparative reproducibility wording; retain motivation and explicit study limitations only.
3. `Done` Describe the integrated Rust/Python implementation accurately while keeping current measurements explicitly non-paper-facing.
4. `Done` State that Docker Compose is a reproducible isolated local deployment, not evidence of independent operators.
5. `Done` Separate the conditional mathematical bound CLM-10 from the unimplemented system premise CLM-11.

## Evidence Update Rule

Every implementation, experiment, proof, citation verification, or manuscript change affecting these claims must update this matrix in the same change. A claim moves to `Supported` only when the cited evidence matches its exact adversary, deployment, and failure assumptions.
